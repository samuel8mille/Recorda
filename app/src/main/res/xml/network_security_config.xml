<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is allowed ONLY for local development hosts so the app can reach a sync
  backend running on the dev machine (10.0.2.2 = host localhost from the emulator). All other
  traffic keeps the platform default (HTTPS required), so production is unaffected.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
