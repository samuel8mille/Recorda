<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution">

    <!--
    Módulo dinâmico da tela de estudo, com entrega install-time (sempre instalado
    junto com o APK base), seguindo o mesmo padrão de :feature:review_session.
    -->
    <dist:module
        dist:instant="false"
        dist:title="@string/title_study">
        <dist:delivery>
            <dist:install-time />
        </dist:delivery>
        <dist:fusing dist:include="true" />
    </dist:module>

    <application>
        <!--
        Registers StudyScreen into StudySessionEntry before Application.onCreate().
        This lets :app navigate to StudyScreen without importing it directly,
        preserving the dynamic-feature dependency direction.
        -->
        <provider
            android:name=".StudySessionInitProvider"
            android:authorities="${applicationId}.study_init"
            android:exported="false" />
    </application>

</manifest>
